# BSEDRC frontend

React/Vite client for the Student Zone. Set `VITE_API_BASE_URL` to the backend origin (the local `.env` uses `http://localhost:3000`).

```text
npm run dev
npm run lint
npm run build
```
